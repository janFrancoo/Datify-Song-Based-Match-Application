<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:paddingHorizontal="15dp"
    android:paddingVertical="5dp" >

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/userSelectListAvatar"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_marginTop="8dp"
        android:src="@drawable/defaultavatar"
        app:civ_border_color="@color/colorPrimaryDark"
        app:civ_border_width="2dp" />

    <TextView
        android:id="@+id/userSelectListUsername"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/userSelectListAvatar"
        android:layout_alignStart="@+id/userSelectListAvatar"
        android:layout_alignEnd="@+id/userSelectListAvatar"
        android:textAlignment="center"
        android:layout_marginTop="16dp"
        android:text="username" />

    <TextView
        android:id="@+id/userSelectListBio"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/userSelectListUsername"
        android:layout_alignStart="@+id/userSelectListAvatar"
        android:layout_alignEnd="@+id/userSelectListAvatar"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="16dp"
        android:maxWidth="160dp"
        android:text="Bio"
        android:textAlignment="center" />

</RelativeLayout>